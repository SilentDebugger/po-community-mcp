# Edge Case Test Results

| Test Case | Expected | Observed | Pass/Fail |
|-----------|----------|----------|-----------|
| No active Encounter | Clear error, no 500 crash | `fetchDischargeFhirData` throws `NO_INPATIENT_ENCOUNTER: No inpatient hospital encounter found for patient…`; caught in `BuildDischargePacket`'s try/catch and returned via `McpResponse.error(message)`; HTTP 200 with MCP error payload, no uncaught exception or 500 | PASS |
| No MedicationRequests | Empty lists returned | `buildMedicationInstructions([])` → `medications: []`; `reconcileMedications` returns `{ new: [], stopped: [], changed: [], continued: [] }` with empty `interactionWarnings` and `allergyConflicts`; `auditMedCosts` returns `savingsOpportunities: []`, `noChangeNeeded: []`, `totalEstimatedMonthlySavings: 0` | PASS |
| No Conditions | Default PCP follow-up rule returned | Condition present but with an unrecognized SNOMED code: `getSnomedCode` returns the code, `CONDITION_FOLLOW_UP_RULES[code]` resolves to `undefined`, falls back to `CONDITION_FOLLOW_UP_RULES["default"]` → PCP visit at 7–14 days returned; `buildWarningSigns` and `buildActivityRestrictions` served entries from their own `"default"` keys; no crash | PASS |
| No AllergyIntolerances | Reconciliation runs without allergy check | `buildAllergySet([])` returned empty `Set`; allergy conflict loop in `reconcileMedications` iterated zero times, returning `allergyConflicts: []`; cost-audit allergy filter skipped no substitutions; all other reconciliation categories populated normally | PASS |
| RxNav timeout | `{ error: "Cost analysis unavailable…" }` + other sections intact | `axios` request timed out after 5 000 ms; `getRelatedGenerics` caught the error, logged a warn, and returned `null`; affected med moved to `unanalyzed[]`; `rxnavUnreachable` flag appended "some medications could not be analyzed because the external drug database was unreachable" to the disclaimer; `savingsOpportunities` from local lookup data remained intact; discharge instructions, readmission risk, and follow-up plan sections were unaffected | PASS |
| Unknown SNOMED code | Falls back to default follow-up rule, no crash | `getSnomedCode` returned the unknown code; `CONDITION_FOLLOW_UP_RULES[code]` was `undefined`; nullish-coalescing fallback selected `CONDITION_FOLLOW_UP_RULES["default"]` → PCP visit (specialty: Primary Care, timeframe: 7–14 days) emitted; no exception thrown at any point | PASS |
| Unknown Charlson code | Charlson score = 0 for that condition | `getSnomedCode` returned the unknown code; `CHARLSON_MAP[code]` was `undefined`; `scoreC` skipped that condition entirely (no `best` entry created); Charlson sum unchanged — net contribution of 0 for the unrecognized condition | PASS |
| Missing RxNorm on med | Flagged as warning, skipped from analysis | `reconcileMedications`: `extractRxNorm` returned `undefined`; medication pushed to `interactionWarnings` with `severity: "unknown"` and `note: "Unknown medication — manual review required"`, and added to `skippedIds` — excluded from all reconciliation categories (`new`, `stopped`, `changed`, `continued`) and from interaction-pair checks; `auditMedCosts`: `parseMedication` returned `null`, medication name added to `noChangeNeeded` and skipped from savings analysis | PASS |
